/**
 * Presentation-only: typing speed, wait timing, animation pacing.
 * Must NOT contain: command names, response selection, output text, fallback behavior.
 */
export const LINE_STAGGER_MS = 60
export const PROGRESS_BAR_ANIMATION_MS = 900